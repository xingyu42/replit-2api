entrypoint = "artifacts/api-server/src/index.ts"

run = "pnpm --filter @workspace/api-server start"

[deployment]
build = "pnpm run build"
run = "pnpm --filter @workspace/api-server start"
ignorePorts = false

[nix]
channel = "stable-23_11"

[[ports]]
localPort = 3000
externalPort = 80
